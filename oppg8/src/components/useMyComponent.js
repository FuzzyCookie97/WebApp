// import { useCallback, useState } from 'react'

// export const MyComponent = (initialValue = 'testing') => {
// //   const [click, setClick] = (initialValue)

// //   const trueEvent = useCallback(() => setClick(click => click = 'true'), [setClick])
// //   const falseEvent = useCallback(() => setClick(click => click = 'false'), [setClick])

//   return{
//       click: initialValue,
//       trueEvent,
//       falseEvent
//   }
// };

//Tried hooks, didn't get it to work. Fuck this